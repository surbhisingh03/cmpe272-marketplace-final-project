# Vite loads this file from the `client/` folder when you run `npm run dev` here.
# Copy to `.env` and fill in. Restart the dev server after any change.

# Groq API key for ShopVerse AI chat (browser). Create a key at https://console.groq.com/
VITE_GROQ_API_KEY=
